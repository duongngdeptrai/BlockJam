using UnityEngine;

public enum ColorType
{
    Red,
    Green,
    Blue,
    Yellow,
    Purple,
    Orange,
    None
}

public enum Direction
{
    Up,
    Down,
    Left,
    Right
}

public enum GameState
{
    Home,
    Playing,
    Win,
    Lose
}